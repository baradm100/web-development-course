@model web_development_course.Models.Branch

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Branch</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
            <p class="font-weight-light">@Model.Name</p>
            </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Address.City)
            <p class="font-weight-normal">@Model.Address.City</p>
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Address.Street)
            <p class="font-weight-normal">@Model.Address.Street</p>
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Address.BuildingNumber)
            <p class="font-weight-normal">@Model.Address.BuildingNumber</p>
        </dt>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
